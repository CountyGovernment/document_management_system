language: node_js
sudo: required  
dist: trusty
node_js:
  - stable
services:
  - postgresql
addons:
  postgresql: “9.4"
before_install:
  - npm install sequelize-cli -g
script:
  - npm test
after_script:
  - npm run coverage